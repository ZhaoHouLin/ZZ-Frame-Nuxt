apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: zz-frame-cert
spec:
  secretName: zz-frame-tls
  issuerRef:
    name: selfsigned-issuer
    kind: ClusterIssuer
  dnsNames:
    - nuxt.example.internal
