apiVersion: v1
kind: Service
metadata:
  labels:
    app: zz-frame
  name: zz-frame
spec:
  ports:
    - port: 80
      protocol: TCP
      targetPort: 3000
  selector:
    app: zz-frame
  type: ClusterIP
